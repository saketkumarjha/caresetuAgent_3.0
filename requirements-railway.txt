# Railway Optimized Requirements - Minimal Memory Footprint
# Core LiveKit and Voice Processing
livekit==1.0.11
livekit-agents==1.2.1
livekit-api==1.0.3
livekit-plugins-assemblyai==1.2.1
livekit-plugins-google==1.2.1

# Essential AI Services (lightweight versions)
google-genai==1.26.0
google-api-python-client==2.176.0
google-auth==2.40.3
google-auth-oauthlib==1.2.2

# Core Python Libraries
aiohttp==3.12.14
asyncio
requests==2.32.4
python-dotenv==1.1.1
PyJWT==2.10.1

# Minimal Data Processing
pydantic==2.11.7
orjson==3.11.0
python-dateutil==2.9.0.post0
pytz==2025.2

# Basic Document Processing (lightweight)
PyPDF2==3.0.1
pdfplumber==0.11.7

# Essential Utilities
click==8.2.1
coloredlogs==15.0.1
structlog==25.4.0
tenacity==8.5.0

# Remove heavy dependencies:
# - chromadb (vector database - too heavy)
# - torch (ML framework - too heavy) 
# - transformers (NLP models - too heavy)
# - sentence-transformers (embeddings - too heavy)
# - scikit-learn (ML library - too heavy)
# - numpy/scipy (scientific computing - too heavy)
# - onnxruntime (ML runtime - too heavy)
# - pinecone-client (vector DB - not needed)
# - weaviate-client (vector DB - not needed)
# - opentelemetry (monitoring - not essential)
# - kubernetes (not needed for Railway)